# Prompt Optimization Changelog

| Epoch | Git SHA | Prompt Hypothesis | Pass Rate | Code Suggestions | Notes |
|-------|---------|-------------------|-----------|--------------------|-------|
| 1 | 8240090 | Re-add corrected forbidden-substitutions table: fix čnosťi→čnosti typo; add vášeň and princíp entries; rephrase tvar entry to avoid forma/tvar constraint conflict (remove blanket "NEVER use forma"); reviewer: add tie-break rule preferring APPROVED_WITH_NOTES when borderline | 88.0% (88/100, run 13) | `prechecks.py::_oov_stem`: add `elif w.endswith("en") and len(w) >= 5: stem = w[:-2]` to cover vášeň→vášne/vášni false rejections (normalized "vasen" stem doesn't prefix-match "vasne") | Run 13 regressed vs run 12 (91.8%→88.0%); root causes: čnosť typo in run-13 prompt (+5), vášeň missing from list (+6), forma/tvar conflict (+1); persistent stuck terms (viera 6→6, starostlivosť 6→6) likely burn iteration budget due to co-occurring failures |
| 2 | bdef8d0 | translator: add completeness rule — each `<term>` entry is independently required; instruct model to scan all constraints before submitting; reviewer: tighten verdict format with explicit colon+space rule and examples to prevent unparseable output | 86.0% (86/100, run 15) | (1) `loop.py`: on `reviewer_error`, retry the reviewer call once before marking needs_human — 2 new errors in run 15 are likely transient API failures, not semantic failures; permanent block wastes a correctly-translated segment. (2) `prechecks.py::_oov_stem`: terms stuck at same failure count across 3+ consecutive runs (princíp=7, pamäť=6, starostlivosť=6, viera=6) suggest stem-matching misses entire inflectional classes; audit coverage for -ip/-íp, -äť, -osť, -a declension patterns and add missing elif branches | Run 14→15: reviewer_revision ▼26→19 (major win from prior prompt work); reviewer_error ▲0→2 (new issue, addressed here); forma/intencionálny obraz fully resolved; milosť ▲0→3 is sample-size noise (97 vs 100 segments processed); stuck terms unchanged — code fix required, not prompt |
| 3 | fce541f | translator: add MODALITY section — preserve necessity/possibility modal strength exactly (debet→musí, necesse est→je nevyhnutné, never weaken to môže); add third example illustrating modal preservation with dual constraints; reviewer: tighten modal-collapse criterion — distinguish modal shift that reverses argument (REVISION_NEEDED) from stylistic reformulation of same necessity (APPROVED_WITH_NOTES) | 91.0% (91/100, run 16) | (1) `loop.py` lines 391–397: on `reviewer_error`, retry the reviewer call once before breaking — still outstanding from epoch 2; reviewer_error ▼2→1 but 1 remaining wastes a correctly-translated segment. (2) `prechecks.py::_word_in_draft`: add `sys.stderr` debug line when stem-prefix fallback fires, logging `(word, stem, matched_token)` — needed to distinguish synonym substitution from genuine stemmer gap for stuck terms (pamäť ▲6→9 likely sample expansion from 97→100 segs, not prompt regression) | Run 15→16: reviewer_revision ▼19→14, avg_iters ▼1.45→1.40, 4 segments improved; pamäť ▲6→9 and 3 new single-failure terms (múdrosť, sila, svedomitosť) coincide exactly with 3 new segments added — attributed to sample expansion, not epoch 2 regression; 20+ precheck terms stuck at flat counts require code-level stemmer fix, not prompt work |
| 4 | fe07b4f | translator: clarify multi-word required_slovak semantics — each component word is an independent constraint, model must verify every word separately not the phrase holistically; reviewer: add demonstrability test before REVISION_NEEDED — reviewer must be able to cite the specific failing clause in one sentence, otherwise use APPROVED_WITH_NOTES | 92.0% (92/100, run 18) | `prechecks.py::_word_in_draft`: after `forms = generate_slovak_forms(w)`, add `if not forms: print(f"[PRECHECK] OOV: no MorphoDiTa forms for '{word}' — stem fallback only", file=sys.stderr)` to distinguish vocabulary gaps from stemmer misses; enables root-cause diagnosis of terms stuck at 3 or 6 failures across multiple epochs (pamäť=6, starostlivosť=6, viera=6) | Run 17→18 (epoch 3 revert): pass rate ▲90.5%→92.0%, 5 segments improved, 0 regressed; failure-class explosion (0→3 per term) is 4 new needs_human segments in expanded 95→100 sample — each failing all 3 iterations; vášeň ▼7→4, čnosť ▼11→9, úcta ▼2→0 confirm revert was correct; remaining reviewer_revision=21 is target for epoch 4 demonstrability tightening |
| 5 | 9612036 | translator: add third example with multi-word constraint (bonum commune → spoločné dobro) showing both component words inflected in locative in the output — makes epoch 4 verbal rule concrete via a visual example; reviewer: narrow grammar-error criterion from "propositional content" (broad) to "argument's main conclusion", explicitly excluding subordinate-clause-only ambiguity as LEGIBILITY — extends demonstrability-test principle to grammar-triggered revisions | 92.0% (92/100, run 19) | (1) `prechecks.py::generate_slovak_forms`: 14 terms flat at 3 or 6 failures across all 5 epochs (pamäť, starostlivosť, viera, duša, etc.) — these are definitively code-level; audit whether MorphoDiTa form generation covers full declension paradigms for ť/ň-stem nouns and soft-stem adjectives; add `if not forms: print(f"[PRECHECK OOV] no forms for '{lemma}'", file=sys.stderr)` to distinguish OOV from partial coverage. (2) čnosť ▲9→11 and prirodzenosť ▲0→3 despite fixed sample: run pilot with `PILOT_WORKERS=5` and capture stderr to confirm which of these terms hit stem-fallback vs. form generation, then patch the specific morphological gap rather than guessing from aggregate counts | Run 18→19 (epoch 4 candidate): pass rate flat 92%→92%; reviewer_revision ▼21→20 (demonstrability test working); avg_iters ▲1.37→1.41 (marginal cost increase); 14 terms permanently stuck at 3 or 6 — no prompt epoch has moved them; all remaining prompt headroom is in reviewer false-positive reduction; code fix to form generation is the only lever that can reduce stuck-term count |
| 6 | 0872513 | translator: distinguish two multi-word required_slovak cases — content phrases (nouns/adjectives: inflect each word independently) vs. formulaic phrases (contains verb/conjunction/preposition: reproduce verbatim); replace holistic scan instruction with sequential per-entry mechanical verification; no reviewer changes (reviewer_revision ▼20→12 confirms epoch 5 is working) | 91.0% (91/100, run 20) | (1) `prechecks.py::generate_slovak_forms`: vášeň ▲3→8 (+5) coincides with 3 newly-regressed segments each failing all 3 iterations — add `if not forms: print(f"[PRECHECK OOV] no forms for '{lemma}'", file=sys.stderr)` (outstanding since epoch 2); audit ň-stem noun paradigm coverage (vášeň→vášne/vášni/vášňou/vášní); (2) `prechecks.py::_word_in_draft`: add `sys.stderr` debug line when stem-prefix fallback fires — needed to distinguish synonym substitution from stemmer gap in the 3 regressed segments | Run 19→20: reviewer_revision ▼20→12 (epoch 5 grammar-error narrowing confirmed effective — do not modify reviewer); pass rate ▼92→91 (3 regressions, 2 improvements; net −1); vášeň ▲3→8 and úcta ▲0→3 and milosť ▲0→2 are downstream of 3 newly-stuck segments exhausting all 3 iterations — code-level stemmer issue; new failure class precheck_terminology(Odpovedám: treba povedať, že) indicates formulaic verb-clause not reproduced verbatim — addressed by epoch 6 clausal-phrase rule; single-term ▲0→1 deltas are sample-size noise |
