DATABASE_URL=postgresql://aquinas:aquinas@localhost:5432/aquinas
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
GSHEETS_SPREADSHEET_ID=
FLASK_SECRET_KEY=change-me
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Daily comment digest (src/notify). Sent via the Resend HTTP API (not SMTP —
# Railway blocks outbound SMTP ports). Sign up free at resend.com, create an
# API key, and verify a sending domain (Resend dashboard -> Domains -> add DNS
# records) so MAIL_FROM can be e.g. "Aquinas Pipeline <digest@your-domain>".
RESEND_API_KEY=
MAIL_FROM=
PUBLIC_BASE_URL=
